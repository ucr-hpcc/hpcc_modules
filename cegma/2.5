#%Module1.0#####################################################################
##
## maui modulefile
##
proc ModulesHelp { } {
        global version prefix

        puts stderr "\tcegma - Adds cegma to your path"
}

module-whatis	"cegma-2.5"

set      iigb_modules    $::env(IIGB_MODULES)
set      modules_dir     $::env(MODULES_DIR)

# for Tcl script use only
set	version		3.2.9
set	prefix		${modules_dir}/${version}
set	exec_prefix	${prefix}
set	datarootdir	${prefix}/share


prepend-path    PATH            ${iigb_modules}/cegma/2.5/bin
prepend-path    PERL5LIB    	${iigb_modules}/cegma/2.5/lib
setenv	CEGMA ${iigb_modules}/cegma/2.5
setenv	CEGMATMP	/scratch
module		use		${modules_dir}

module load hmmer/3.0
module load genewise
module load SNAP
module load geneid
module load ncbi-blast/2.2.25+

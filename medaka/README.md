the nanopore polishing of assemblies tool medaka.

This is installed in a miniconda3 env `medaka` and depends on racon, minimap2 and others

A GPU version is installed in the miniconda3 env `medaka-gpu`

#%Module1.0#####################################################################
##
## maui modulefile
##
proc ModulesHelp { } {
        global version prefix

        puts stderr "\tmira - Adds mira to your path"
}

module-whatis   "mira-4.0.2"

set      iigb_modules    $::env(IIGB_MODULES)
set      modules_dir     $::env(MODULES_DIR)

# for Tcl script use only
set     version         3.2.9
set     prefix          ${modules_dir}/${version}
set     exec_prefix     ${prefix}
set     datarootdir     ${prefix}/share



prepend-path    PATH            ${iigb_modules}/mira/4.0.2/bin
prepend-path    PATH            ${iigb_modules}/mira/4.0.2/scripts
prepend-path    MANPATH         ${iigb_modules}/mira/4.0.2/docs

module          use             ${modules_dir}


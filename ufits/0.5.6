#%Module1.0#####################################################################
##
## ufits modulefile
##
proc ModulesHelp { } {
        global version prefix

        puts stderr "\tufits - Adds ufits ITS pipeline to your path"
}

module-whatis	"ufits-0.5.5"

set      iigb_modules    $::env(IIGB_MODULES)
set      modules_dir     $::env(MODULES_DIR)

# for Tcl script use only
set	version		3.2.9
set	prefix		${modules_dir}/${version}
set	exec_prefix	${prefix}
set	datarootdir	${prefix}/share


module load usearch/8
module load ITSx
module load hmmer
module load fastx_toolkit

#prepend-path	PYTHONPATH	${iigb_modules}/ufits/0.5.5/lib
prepend-path	PATH	${iigb_modules}/ufits/0.5.5
module		use		${modules_dir}

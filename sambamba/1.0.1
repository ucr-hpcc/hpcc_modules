#%Module1.0#####################################################################
##
## Sambamba modulefile
##
proc ModulesHelp { } {
        global version prefix

        puts stderr "\t - Adds Sambamba to your path"
}

module-whatis   "sambamba-1.0.1"

set      hpcc_modules    $::env(HPCC_MODULES)

# Setup PATHs
prepend-path    PATH            ${hpcc_modules}/sambamba/1.0.1/bin


#%Module1.0#####################################################################
##
## maui modulefile
##
proc ModulesHelp { } {
        global version prefix

        puts stderr "\tmeme - Adds meme to your path"
}

module-whatis	"meme-5.0.3"

set      iigb_modules    $::env(IIGB_MODULES)
set      modules_dir     $::env(MODULES_DIR)

# for Tcl script use only
set	version		3.2.9
set	prefix		${modules_dir}/${version}
set	exec_prefix	${prefix}
set	datarootdir	${prefix}/share


prepend-path    PATH            ${iigb_modules}/meme/5.0.3/bin
prepend-path    PERL5LIB        ${iigb_modules}/meme/5.0.3/lib/meme-5.0.2/perl
prepend-path    PYTHONPATH      ${iigb_modules}/meme/5.0.3/lib/meme-5.0.2/python

module		use		${modules_dir}


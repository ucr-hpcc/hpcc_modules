#%Module1.0#####################################################################
##
## barrnap modulefile
##
proc ModulesHelp { } {
        global version prefix

        puts stderr "\tbarrnap. Adds barrnap rRNA gene predictor to your path"
}

module-whatis	"barrnap-0.4.1"

# for Tcl script use only
set	version		3.2.9
set	prefix		/usr/local/Modules/3.2.9
set	exec_prefix	${prefix}
set	datarootdir	${prefix}/share

prepend-path	PATH	/opt/barrnap/0.4.1/bin
module		use		/usr/local/Modules/versions

#%Module1.0#####################################################################
##
## RDPTools modulefile
##
proc ModulesHelp { } {
        global version prefix

        puts stderr "\t - Sets RDPTOOLS jar path to your environment"
}

module-whatis   "RDPTools-2.0.2"

set      hpcc_modules    $::env(HPCC_MODULES)
set      modules_dir     $::env(MODULES_DIR)

# for Tcl script use only
set     version         3.2.9
set     prefix          ${modules_dir}/${version}
set     exec_prefix     ${prefix}
set     datarootdir     ${prefix}/share

module load java/8

setenv          RDPTOOLSDIR	${hpcc_modules}/RDPTools/2.0.2/lib
setenv          RDPTOOLS	${hpcc_modules}/RDPTools/2.0.2
prepend-path    CLASSPATH	${hpcc_modules}/RDPTools/2.0.2/lib

# mostly for QIIME 
setenv	RDP_JAR_PATH	${hpcc_modules}/RDPTools/2.0.2/lib/classifier.jar


#%Module1.0#####################################################################
##
## modkit modulefile
##
proc ModulesHelp { } {
        global version prefix

        puts stderr "\tmodkit- Adds modkit to your path"
}

module-whatis	"modkit-0.5.1"

set      hpcc_modules    $::env(HPCC_MODULES)

module load samtools
prepend-path    PATH            ${hpcc_modules}/modkit/0.5.1/

